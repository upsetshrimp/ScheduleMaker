class Event:
	def __init__(self, length, min_per_week, max_per_week):
		self.length = length
		self.min_per_week = min_per_week
		self.max_per_week = max_per_week
	def value():
		return self.length, self.min_per_week, self.max_per_week